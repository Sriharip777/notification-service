package com.tutoring.notification.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class GmailConfig {
    // Gmail SMTP is auto-configured via application.yml
}
